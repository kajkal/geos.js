---
sidebar_label: 'initialize'
sidebar_position: 2
---

# initialize

Initializes `geos.js` module using `fetch` request or already compiled module.

Note that the returned module is just stateless, compiled WebAssembly code.
`WebAssembly.Module` could be shared with another worker or window via `postMessage`.

## Parameters

| Name | Type | Description |
| --- | :-: | --- |
| `source` | [**`Response`**](https://developer.mozilla.org/en-US/docs/Web/API/Response) \| [**`Promise`**](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<[**`Response`**](https://developer.mozilla.org/en-US/docs/Web/API/Response)\> \| [**`WebAssembly.Module`**](https://developer.mozilla.org/en-US/docs/WebAssembly/Reference/JavaScript_interface/Module) | Either a .wasm file fetch or already compiled module |

## Returns

[**`Promise`**](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<[**`WebAssembly.Module`**](https://developer.mozilla.org/en-US/docs/WebAssembly/Reference/JavaScript_interface/Module)\> A Promise that resolves with the `WebAssembly.Module`

## See also

- [`terminate`](terminate.mdx) terminates initialized `geos.js` instance

## Examples

```ts title="browser; directly from fetch request"
await initialize(fetch('geos_js.wasm'));
```
```ts title="browser; from compiled module"
const module = await WebAssembly.compileStreaming(fetch('geos_js.wasm'));
await initialize(module);
```
```ts title="node; from a local file"
const wasmData = await readFile('./geos_js.wasm');
const module = await WebAssembly.compile(wasmData);
await initialize(module);
```
