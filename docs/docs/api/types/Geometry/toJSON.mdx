---
sidebar_label: 'toJSON'
sidebar_position: 6
---

# Geometry.prototype.toJSON

Converts the geometry to a GeoJSON geometry object.
This method allows the geometry to be serialized to JSON
and is automatically called by `JSON.stringify()`.

## Returns

[**`GeoJSONGeometry`**](https://datatracker.ietf.org/doc/html/rfc7946#section-3.1) A GeoJSON geometry representation of this geometry

## Examples

```js live noInline
// converting a geometry to GeoJSON
const geom = point([ 1, 2, 3 ]);
const geojson = geom.toJSON(); // {"type":"Point","coordinates":[1,2,3]}
const geojsonStr = JSON.stringify(geom); // '{"type":"Point","coordinates":[1,2,3]}'

// this example is alive!
render(preview({ geom, geojson, geojsonStr }));
```
